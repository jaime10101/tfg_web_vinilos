# ============================================================
# .gitignore — Vinyl Sounds TFG
# ============================================================

# ── SECRETOS — NUNCA subir ───────────────────────────────────
.env
.env.local
.env.production
.env.*

# config.js del frontend (si existiera)
js/config.js

# application-local.properties (configuración local Spring Boot)
application-local.properties
application-prod.properties

# ── Spring Boot ───────────────────────────────────────────────
*.class
*.jar
*.war
target/
.mvn/
!.mvn/wrapper/maven-wrapper.jar

# ── IDEs ──────────────────────────────────────────────────────
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# ── Node (por si acaso) ───────────────────────────────────────
node_modules/
npm-debug.log